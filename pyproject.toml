
[project]
name = "clinexa"
version = "1.0"
readme = "README.md"
requires-python = ">=3.10"


dependencies = []

[tool.setuptools]
include-package-data = true
packages = ['dags', 'logs', 'infra', 'config', 'include', 'plugins', 'tests']